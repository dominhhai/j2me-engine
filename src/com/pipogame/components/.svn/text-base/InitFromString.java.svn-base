package com.pipogame.components;

/**
 * Một đối tượng có thể khởi tạo lại các field của nó
 * @author HaiNH
 * @company B-Gate
 */
public interface InitFromString {
    /**
     * Khởi tạo lại các field với các giá trị được lấy trong <code>params</code>
     * @param params chứa các trường của đối tượng mà đối tượng đó có thể 
     * extract và gán cho các field
     */
    public void init(String params);
}
